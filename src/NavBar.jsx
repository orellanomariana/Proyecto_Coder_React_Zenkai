import CartWidget from "./CartWidget";

function NavBar() {
    return (
        <nav>
            <h1>Tienda Online de Zenkai</h1>
            <CartWidget />
        </nav>
    );
}

export default NavBar;